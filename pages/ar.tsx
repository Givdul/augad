import React, { useEffect } from 'react';
import 'aframe';
import 'mind-ar/dist/mindar-image-aframe.prod.js';

const ARPage: React.FC = () => {
    useEffect(() => {
        // Any additional setup can go here
    }, []);

    return (<p>hello world ar page</p>);
};

export default ARPage;